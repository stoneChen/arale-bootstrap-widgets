<div class="form-group {{#if verticalLayout}}vertical-form-group{{/if}}">
    <label class="col-xs-2 control-label {{label.requiredCls}}">{{label.text}}</label>

    <div class="col-xs-{{colWidth}}" style="display: table;">
        <input type="{{type}}" class="form-control" name="{{name}}" value="{{value}}"  {{#if disabled}}disabled{{/if}}/>
             <span class="input-group-addon"><span class="glyphicon-calendar glyphicon"></span>
             </span>
    </div>
</div>